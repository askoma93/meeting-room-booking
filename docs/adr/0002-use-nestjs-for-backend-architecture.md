# Use NestJS for backend architecture

The backend uses NestJS because the first version needs clear module boundaries, role guards, DTO validation, injectable services, and testable booking business rules. This is heavier than a minimal Express or Fastify app, but it better supports the project's goal of demonstrating full-stack architecture and layered testing.
