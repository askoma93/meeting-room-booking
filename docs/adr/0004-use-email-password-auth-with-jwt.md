# Use email/password authentication with JWT

The first version uses email/password registration and JWT access tokens because it provides real authentication and role-based access control without adding an external identity provider. Public registration creates regular users, while administrator accounts are created through seed data or direct operational setup rather than a public endpoint.
